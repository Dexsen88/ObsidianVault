✅ **Correct Answer: C) Using Safe APIs (e.g., array-based execution); because the OS kernel treats the entire user input as a literal, unexecutable argument.**

💡 **Explanation:** Safe APIs structurally prevent the OS shell from parsing the input for command-separating meta-characters, thereby eliminating the **[[Command Injection]]** vulnerability at its root.