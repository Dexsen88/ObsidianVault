✅ **Correct Answer: C) The browser misinterpreted the data as executable code; the missing action was [[Contextual Output Encoding]] (Escaping) for the HTML context.**

💡 **Explanation:** The browser interpreted the angle brackets (`<>`) as HTML tags. **[[Contextual Output Encoding]]** would have converted `<` to `&lt;`, rendering it harmlessly as text.