✅ **Correct Answer: C) It separates the SQL command logic from the user-supplied data, ensuring input is always treated as inert data and can never be executed as a command.**

💡 **Explanation:** **[[Parameterization]]** is the direct technical countermeasure to **[[SQL Injection]]** because it changes the _interpretation_ of the user input, neutralizing its command potential.